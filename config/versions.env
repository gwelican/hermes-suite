# =============================================================================
# config/versions.env — Single source of truth for pinned component versions
# =============================================================================
# CI, local scripts, Docker build args, and Renovate all read this file.
# Release/image tags are generated by CI as YYYY.MM.DD.<run_number>, with an
# additional human-readable alias derived from the pinned component versions.
# =============================================================================

HERMES_AGENT_VERSION=v2026.6.5
HERMES_WEBUI_VERSION=v0.51.426
OBSIDIAN_HEADLESS_VERSION=0.0.10

AGENTMEMORY_VERSION=0.9.26
AGENTMEMORY_III_VERSION=0.11.2

NODE_BASE_IMAGE=ghcr.io/gwelican/node:24-bookworm-slim

CONTAINER_RUNTIME=auto
USE_SUDO=false
